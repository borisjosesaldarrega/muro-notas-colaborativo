# Frontend

La interfaz del Muro Colaborativo está en `public/`. Incluye las páginas, estilos, recursos y el cliente público que se ejecutan en el navegador.

Esta carpeta nunca debe contener claves privadas ni operaciones administrativas.
