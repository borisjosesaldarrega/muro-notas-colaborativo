# Backend

Contiene el servidor Express y Socket.io, la persistencia y los scripts de mantenimiento. Se ejecuta desde la raíz con `npm start` o `npm run dev`.

Las operaciones administrativas de Supabase pertenecen exclusivamente a esta capa y leen sus credenciales privadas desde variables de entorno.
